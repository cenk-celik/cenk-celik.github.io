scholarly>=1.7.11
# scholarly's proxy support needs httpx's SOCKS extra; without this,
# `from scholarly import ProxyGenerator` raises ImportError at import time.
socksio
# No free-proxy package dependency here on purpose: fetch_publications.py
# scrapes free-proxy-list.net itself (see _scrape_candidate_proxies()
# there) rather than going through either scholarly's own
# ProxyGenerator.FreeProxies() or the free-proxy package, both of which
# hit real bugs/dead ends. That scraping uses `requests` and
# `beautifulsoup4`, which are already hard dependencies of scholarly
# itself, so nothing new is needed here for it.
