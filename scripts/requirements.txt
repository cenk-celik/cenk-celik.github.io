# fetch_publications.py is the only script here with an external
# dependency - a plain HTTP GET to Crossref's REST API. fetch_repos.py
# and fetch_bluesky.py use only Python's standard library (urllib).
requests
