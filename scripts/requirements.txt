scholarly>=1.7.11
# scholarly's proxy support needs httpx's SOCKS extra; without this,
# `from scholarly import ProxyGenerator` raises ImportError at import time.
socksio
# fetch_publications.py imports fp.fp/fp.errors directly (see
# _find_working_proxy() there) instead of going through scholarly's own
# ProxyGenerator.FreeProxies(), which hit two separate real bugs in its
# internal proxy-cycling generator. No version pin needed here: unlike
# that internal generator, free-proxy's own public get() has always
# handled an empty scrape safely, on every version.
free-proxy
